# Changelog

## [0.1.0] - Unreleased

### Added
- Project scaffolding and spec
- YAML config schema with pydantic validation
- Salesforce source connector (MVP)
- CloudZero unit metric telemetry client
- Record transform engine with flexible dimension mapping
- CLI: `sync`, `validate`, `version` commands
- Historical backfill and monthly sync modes
- Example config files
